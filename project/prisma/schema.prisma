datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output   = "./generated/prisma"
}

model orders {
  id        Int      @id @default(autoincrement())
  wheels Int
  windshield_wipers     Int
  radios      Int
}